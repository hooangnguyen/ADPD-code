<link rel="stylesheet" href="~/css/Login.css" />

<section class="login-page">
    <div class="login-container">
        <div class="login-header">
            <div class="icon">📚</div>
            <h1>Đăng nhập</h1>
            <p>Hệ thống Quản lý Sinh viên</p>
        </div>

        <div class="login-body">
            <!-- Hiển thị thông báo thành công từ TempData -->
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success" style="background: #d4edda; border: 1px solid #c3e6cb; color: #155724; padding: 12px; border-radius: 8px; margin-bottom: 20px;">
                    @TempData["SuccessMessage"]
                </div>
            }

            <!-- Hiển thị lỗi validation -->
            @if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
            {
                <div class="error-message alert alert-danger" style="display: block;">
                    @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                </div>
            }

            <form method="post" asp-controller="Login" asp-action="Login">
                @Html.AntiForgeryToken()

                <div class="form-group">
                    <label for="username">Tên đăng nhập hoặc Mã sinh viên</label>
                    <div class="input-wrapper">
                        <input type="text"
                               name="username"
                               id="username"
                               placeholder="Nhập tên đăng nhập"
                               required>
                        <span class="input-icon">👤</span>
                    </div>
                </div>

                <div class="form-group">
                    <label for="password">Mật khẩu</label>
                    <div class="input-wrapper">
                        <input type="password"
                               name="password"
                               id="password"
                               placeholder="Nhập mật khẩu"
                               required>
                        <span class="input-icon">🔒</span>
                    </div>
                </div>

                <div class="form-options">
                    <label class="remember-me">
                        <input type="checkbox" name="remember" id="remember">
                        <span>Ghi nhớ đăng nhập</span>
                    </label>
                    <a href="@Url.Action("Index", "Forgotpassword")" class="forgot-password">Quên mật khẩu?</a>
                </div>

                <button type="submit" class="btn-login">Đăng nhập</button>
            </form>

            <div class="divider">
                <span>hoặc</span>
            </div>

            <div class="register-link">
                Chưa có tài khoản? <a href="@Url.Action("Index", "Register")">Đăng ký ngay</a>
            </div>
        </div>
    </div>
</section>