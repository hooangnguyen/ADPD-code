@{
    Layout = null;
    var isPartial = ViewData["IsPartial"] as bool? ?? false;
}

@if (isPartial)
{
    @await Html.PartialAsync("_DashboardHomePartial")
}
else
{
    <link rel="stylesheet" href="~/css/Dashboard.css" />

    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <a href="@Url.Action("Dashboard", "Student")" class="sidebar-logo">
                📚 StudentMS
            </a>
        </div>

        <div class="user-profile">
            <a href="@Url.Action("Profile", "Student")"
               data-partial-url="@Url.Action("Profile", "Student", new { partial = true })">
                <div class="user-avatar">👤</div>
                <div class="user-info">
                    <h3>@ViewBag.FullName</h3>
                    <p>@(ViewBag.StudentId ?? "N/A")</p>
                </div>
            </a>
        </div>
        <ul class="menu">
            <li class="menu-item">
                <a href="@Url.Action("Dashboard", "Student")"
                   class="active"
                   data-partial-url="@Url.Action("Dashboard", "Student", new { partial = true })">
                    <span class="menu-icon">🏠</span>
                    <span>Trang chủ</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="@Url.Action("Grades", "Student")"
                   data-partial-url="@Url.Action("Grades", "Student", new { partial = true })">
                    <span class="menu-icon">📊</span>
                    <span>Điểm số</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="@Url.Action("Schedule", "Student")"
                   data-partial-url="@Url.Action("Schedule", "Student", new { partial = true })">
                    <span class="menu-icon">📅</span>
                    <span>Lịch học</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="#">
                    <span class="menu-icon">📚</span>
                    <span>Môn học</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="@Url.Action("RegisterStudy", "Student")"
                   data-partial-url="@Url.Action("RegisterStudy", "Student", new { partial = true })">
                    <span class="menu-icon">✏️</span>
                    <span>Đăng ký học</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="@Url.Action("Assignments", "Student")"
                   data-partial-url="@Url.Action("Assignments", "Student", new { partial = true })">
                    <span class="menu-icon">📝</span>
                    <span>Bài tập</span>
                </a>
            </li>
            <li class="menu-item">
                <a href="@Url.Action("Logout", "Login")">
                    <span class="menu-icon">🚪</span>
                    <span>Đăng xuất</span>
                </a>
            </li>
        </ul>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div id="dynamic-content"
             data-default-url='@Url.Action("Dashboard", "Student", new { partial = true })'>
            @await Html.PartialAsync("_DashboardHomePartial")
        </div>
    </main>

    <script src="~/js/Dashboard.js" asp-append-version="true"></script>
}

