@{
    var isPartial = ViewData["IsPartial"] as bool? ?? false;
}

<link href="~/css/AdminDashboard.css?v=1.0" rel="stylesheet" asp-append-version="true" />

<div class="admin-dashboard-wrapper">
    <div class="dashboard-header">
        <h1>
            <span style="font-size: 36px;">🏠</span>
            Admin Dashboard
        </h1>
        <p>Manage the entire system from here</p>
    </div>

    <div class="stats-grid">
        <div class="stat-card stat-students">
            <div class="stat-icon">🎓</div>
            <h3>@(ViewBag.TotalStudents ?? 0)</h3>
            <p>Students</p>
        </div>
        <div class="stat-card stat-lecturers">
            <div class="stat-icon">👩‍🏫</div>
            <h3>@(ViewBag.TotalLecturers ?? 0)</h3>
            <p>Lecturers</p>
        </div>
        <div class="stat-card stat-courses">
            <div class="stat-icon">📚</div>
            <h3>@(ViewBag.TotalCourses ?? 0)</h3>
            <p>Courses</p>
        </div>
        <div class="stat-card stat-classes">
            <div class="stat-icon">🏫</div>
            <h3>@(ViewBag.TotalClasses ?? 0)</h3>
            <p>Classes</p>
        </div>
        <div class="stat-card stat-accounts">
            <div class="stat-icon">🔐</div>
            <h3>@(ViewBag.TotalAccounts ?? 0)</h3>
            <p>Accounts</p>
        </div>
        <div class="stat-card stat-enrollments">
            <div class="stat-icon">📝</div>
            <h3>@(ViewBag.ActiveEnrollments ?? 0)</h3>
            <p>Enrollments</p>
        </div>
    </div>

    <div class="quick-actions">
        <h2>Quick Actions</h2>
        <div class="actions-grid">
            <a href="@Url.Action("Students", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Students", "Admin", new { partial = true })">
                <div class="action-icon">🎓</div>
                <h3>Manage Students</h3>
                <p>Add, edit, delete students</p>
            </a>
            <a href="@Url.Action("Lecturers", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Lecturers", "Admin", new { partial = true })">
                <div class="action-icon">👩‍🏫</div>
                <h3>Manage Lecturers</h3>
                <p>Add, edit, delete lecturers</p>
            </a>
            <a href="@Url.Action("Courses", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Courses", "Admin", new { partial = true })">
                <div class="action-icon">📚</div>
                <h3>Manage Courses</h3>
                <p>Add, edit, delete courses</p>
            </a>
            <a href="@Url.Action("Classes", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Classes", "Admin", new { partial = true })">
                <div class="action-icon">🏫</div>
                <h3>Manage Classes</h3>
                <p>Add, edit, delete classes</p>
            </a>
            <a href="@Url.Action("Accounts", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Accounts", "Admin", new { partial = true })">
                <div class="action-icon">🔐</div>
                <h3>Manage Accounts</h3>
                <p>Create and manage accounts</p>
            </a>
            <a href="@Url.Action("Statistics", "Admin")"
               class="action-card"
               data-partial-url="@Url.Action("Statistics", "Admin", new { partial = true })">
                <div class="action-icon">📊</div>
                <h3>Statistics & Reports</h3>
                <p>View system reports</p>
            </a>
        </div>
    </div>
</div>
