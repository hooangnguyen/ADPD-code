<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ADPD_code</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    @* XLSX library to support Excel export on Lecturer/InformationStudent page *@
    <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
</head>
<body>
    <header>
        <nav class="navbar-custom">
            <div class="nav-container">

                <a href="@Url.Action("Index", "Home")" class="logo">
                    📚 StudentMS
                </a>

                <ul class="nav-links">
                    <li><a href="@Url.Action("Index", "Home")">Home</a></li>
                    <li><a href="#about">About</a></li>
                    <li><a href="#contact">Contact</a></li>
                </ul>
                @await Html.PartialAsync("_LoginStatusPartial")

            </div>
        </nav>
        @* Include with: @Html.Partial("_NotificationBell") inside your header/navbar *@
        <button id="notificationBtn" class="notification-btn" type="button" aria-expanded="false" title="Thông báo">
            <span class="bell-icon">🔔</span>
            <span id="notificationBadge" class="notification-badge" style="display:none">0</span>
        </button>
        <div id="notificationDropdown" class="notification-dropdown" style="display:none"></div>

        <link rel="stylesheet" href="~/css/AdminDashboard.css" asp-append-version="true" />
        <style>
            .notification-btn {
                position: relative;
                background: transparent;
                border: none;
                cursor: pointer;
                font-size: 20px;
                padding: 6px 10px;
                color: inherit;
            }

            .notification-badge {
                display: inline-block;
                min-width: 18px;
                height: 18px;
                padding: 0 5px;
                line-height: 18px;
                border-radius: 9px;
                background: #e74c3c;
                color: white;
                font-size: 12px;
                font-weight: 600;
                position: absolute;
                top: 2px;
                right: 2px;
            }

            .notification-dropdown {
                position: absolute;
                right: 12px;
                top: 46px;
                z-index: 1200;
                display: none;
                min-width: 300px;
            }
        </style>

        <script src="~/js/notifications.js" asp-append-version="true"></script>
    </header>

    <div class="container-fluid">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>